import { styled } from '../css';
import {
    Popover as _Popover,
    Modal as _Modal,
    MaskModal as _MaskModal,
    PopContent as _PopContent,
    PopProps
} from '../../../macoolka-css-components/lib/Pop';
export { PopProps }
export const Popover = styled(_Popover)
export const Modal = styled(_Modal)
export const MaskModal = styled(_MaskModal)
export const PopContent = styled(_PopContent)

